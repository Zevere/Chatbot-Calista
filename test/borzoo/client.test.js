import { Client } from '../../src/borzoo/client';
describe('lalala', () => {
    it('does stuff', () => {
        let foo = 'foo';
    });
    it('can instantiate', () => {
        new Client();
    });
});
